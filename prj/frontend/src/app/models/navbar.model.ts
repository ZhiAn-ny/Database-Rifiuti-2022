export enum enum_AppBarPositionMode {
    STATIC = 'static',
    STICKY = 'sticky',
    FIXED = 'fixed',
}

export enum enum_AppBarColor {
    LIGHT = "light",
    DARK = "dark",
    INHERIT = "inherit"
}
